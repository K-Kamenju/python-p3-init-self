#!/usr/bin/env python3

class Dog:
    def __init__(self, name = "Please Add Name", breed = "Mutt"):
        # self.name = print(name)
        # self.breed = print(breed)
        self.name = name
        self.breed = breed

fido = Dog("Fido")
fido.name
fido.breed